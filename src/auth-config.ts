/**
 * GitHub App 등록 후 채우는 값. 둘 다 공개값이다 —
 * clientId는 승인 URL에 실리고 workerUrl은 네트워크 탭에 보인다.
 * 비밀(client_secret)은 Worker에만 있다.
 */
export const AUTH = {
  clientId: "Iv23licUjzfD7wEKnauT",
  workerUrl: "https://todo-login.zoona.workers.dev",
};
